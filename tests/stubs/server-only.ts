// Stub for Next.js's `server-only` package, which exists to fail the build if
// a server module is imported from a client component. It has no runtime
// behaviour, so unit tests alias it to this empty module.
export {};
